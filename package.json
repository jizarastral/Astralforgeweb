{
  "name": "astralforge-web",
  "version": "1.0.0",
  "description": "AstralForgeAE — fabrication, HVAC G+20, 3D printing, IT & web solutions (UAE)",
  "private": true,
  "scripts": {
    "start": "npx --yes serve -s . -l ${PORT:-3000}",
    "dev": "npx --yes serve . -l 3000",
    "api": "npm --prefix api start",
    "api:dev": "npm --prefix api run dev",
    "api:install": "npm --prefix api install"
  },
  "engines": {
    "node": ">=18"
  }
}
