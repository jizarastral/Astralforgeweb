{
  "name": "astralforge-web",
  "version": "1.0.0",
  "description": "AstralForgeAE — web development, IT solutions & digital growth from 100 AED/day (UAE)",
  "private": true,
  "scripts": {
    "start": "npx --yes serve -s . -l ${PORT:-3000}",
    "dev": "npx --yes serve . -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
