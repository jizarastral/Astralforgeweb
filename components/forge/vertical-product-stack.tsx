/** Re-export: product carousel is now horizontal (X move + rotateY). */
export {
  HorizontalProductCarousel,
  HorizontalProductCarousel as VerticalProductStack,
} from "@/components/forge/horizontal-product-carousel";
